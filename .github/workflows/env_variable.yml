name: environement_demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  envdemo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: env demo
        env:
          APP_ENV: "prod"
        run: echo "App is connecting to $APP_ENV"
