name: Build and Deploy
on:
  pull_request: 
    types: 
      - opened
      - synchronize
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo " The building the application"
